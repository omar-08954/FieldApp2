"use client";
import { useQuery } from "@tanstack/react-query";
import { motion } from "framer-motion";
import { Area, AreaChart, Bar, BarChart, CartesianGrid, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { AlertTriangle, CheckCircle2, ClipboardList, ScanSearch } from "lucide-react";
import { api, Summary } from "@/lib/api";
const empty: Summary = { total_tasks: 0, completion_rate: 0, delayed_tasks: 0, needs_review: 0, by_status: [], daily_trend: [], latest_tasks: [], top_technicians: [] };
const cards = (d: Summary) => [["إجمالي المهام", d.total_tasks, ClipboardList, "من كامل العمليات"], ["معدل الإنجاز", `${d.completion_rate}%`, CheckCircle2, "مهام مكتملة"], ["متأخرة", d.delayed_tasks, AlertTriangle, "تحتاج متابعة"], ["للمراجعة", d.needs_review, ScanSearch, "من الاستيراد"]] as const;
export function Dashboard() { const { data = empty } = useQuery({ queryKey: ["summary"], queryFn: () => api<Summary>("/dashboard/summary") }); return <><section className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="mb-2 text-sm font-medium text-brand">مساحة العمليات</p><h1 className="text-3xl font-bold tracking-tight">صباح الخير، فريق العمل</h1><p className="mt-2 text-slate-500">نظرة حية على الأداء والمهام الميدانية.</p></div></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards(data).map(([label, value, Icon, note], i) => <motion.article initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: i * .06 }} className="metric" key={label}><div className="flex items-start justify-between"><div><p className="text-sm text-slate-500">{label}</p><p className="mt-3 text-3xl font-bold">{value}</p><p className="mt-2 text-xs text-slate-400">{note}</p></div><div className="rounded-xl bg-indigo-50 p-2.5 text-brand dark:bg-indigo-950"><Icon size={20}/></div></div></motion.article>)}</section><section className="mt-5 grid gap-5 xl:grid-cols-[1.7fr_1fr]"><article className="panel"><div className="mb-5"><h2 className="font-bold">الاتجاه اليومي</h2><p className="text-sm text-slate-500">آخر 14 يوماً من العمليات</p></div><div className="h-72"><ResponsiveContainer><AreaChart data={data.daily_trend}><defs><linearGradient id="fill" x1="0" x2="0" y1="0" y2="1"><stop stopColor="#635bff" stopOpacity=".35"/><stop offset="1" stopColor="#635bff" stopOpacity="0"/></linearGradient></defs><CartesianGrid vertical={false} strokeDasharray="3 3"/><XAxis dataKey="label"/><YAxis/><Tooltip/><Area type="monotone" dataKey="value" stroke="#635bff" fill="url(#fill)" strokeWidth={3}/></AreaChart></ResponsiveContainer></div></article><article className="panel"><h2 className="font-bold">توزيع الحالة</h2><p className="mb-2 text-sm text-slate-500">حالة المهام الحالية</p><div className="h-72"><ResponsiveContainer><PieChart><Pie data={data.by_status} dataKey="value" nameKey="label" innerRadius={65} outerRadius={95} fill="#635bff" paddingAngle={4}/><Tooltip/></PieChart></ResponsiveContainer></div></article></section><section className="mt-5 grid gap-5 xl:grid-cols-2"><article className="panel"><h2 className="mb-4 font-bold">أفضل الفنيين</h2><div className="h-64"><ResponsiveContainer><BarChart data={data.top_technicians}><XAxis dataKey="label"/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="value" fill="#635bff" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></article><article className="panel"><h2 className="mb-4 font-bold">أحدث المهام</h2><div className="divide-y">{data.latest_tasks.length ? data.latest_tasks.map(task=><div key={task.id} className="flex items-center justify-between gap-3 py-3 text-sm"><span><span className="font-semibold">{task.task_number}</span><span className="mr-2 text-slate-500">{task.technician_name}</span></span><span className="rounded-full bg-slate-100 px-2 py-1 text-xs dark:bg-slate-800">{task.task_status}</span></div>) : <p className="py-10 text-center text-sm text-slate-500">لا توجد مهام بعد.</p>}</div></article></section></>; }
