"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Bot, LayoutDashboard, Menu, UserRound } from "lucide-react";
import { Assistant } from "./assistant";
const links = [["الرئيسية", "/"], ["لوحة التحكم", "/dashboard"], ["لوحة المدير", "/admin"], ["لوحة الفني", "/technician"], ["التقارير", "/reports"], ["المستودع", "/inventory"], ["المستخدمون", "/users"], ["مركز المطور", "/developer"]] as const;
export function Shell({ children }: { children: React.ReactNode }) { const pathname = usePathname(); return <div className="min-h-screen"><header className="sticky top-0 z-30 border-b bg-white/80 backdrop-blur dark:border-slate-800 dark:bg-slate-950/80"><div className="mx-auto flex max-w-[1600px] items-center gap-4 px-4 py-3 sm:px-7"><Link href="/" className="flex items-center gap-2 font-bold"><span className="grid size-8 place-items-center rounded-lg bg-brand text-white">F</span>FieldApp</Link><nav className="hidden flex-1 items-center justify-center gap-1 lg:flex">{links.map(([label, href]) => <Link key={href} href={href} className={`rounded-lg px-3 py-2 text-sm ${pathname === href ? "bg-indigo-50 font-semibold text-brand dark:bg-indigo-950" : "text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-800"}`}>{label}</Link>)}</nav><div className="mr-auto flex items-center gap-2"><Assistant/><button className="rounded-lg p-2 hover:bg-slate-100 dark:hover:bg-slate-800" aria-label="الحساب"><UserRound size={19}/></button><button className="rounded-lg p-2 lg:hidden" aria-label="القائمة"><Menu size={19}/></button></div></div></header><main className="mx-auto w-full max-w-[1600px] p-4 sm:p-7">{children}</main></div>; }
