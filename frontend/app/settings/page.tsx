"use client";
import { useTheme } from "next-themes";
import { Shell } from "@/components/shell";
export default function SettingsPage(){const {theme,setTheme}=useTheme();return <Shell><div className="max-w-3xl"><p className="text-sm font-medium text-brand">Settings</p><h1 className="mt-2 text-2xl font-bold">الإعدادات</h1><section className="panel mt-6"><h2 className="font-bold">المظهر</h2><p className="mt-1 text-sm text-slate-500">اختر المظهر المناسب لمساحة العمل.</p><div className="mt-4 flex flex-wrap gap-2">{[["light","فاتح"],["dark","داكن"],["system","حسب النظام"]].map(([value,label])=><button key={value} onClick={()=>setTheme(value)} className={`rounded-xl border px-4 py-2 text-sm ${theme===value?"border-brand bg-brand/10 text-brand":"hover:bg-slate-50 dark:hover:bg-slate-800"}`}>{label}</button>)}</div></section><section className="panel mt-5"><h2 className="font-bold">الجلسة والأمان</h2><p className="mt-1 text-sm text-slate-500">يتم تجديد جلسة الدخول تلقائياً باستخدام Refresh Token عند انتهاء Access Token. في النشر العام يوصى بنقل Refresh Token إلى Cookie من نوع HttpOnly عبر طبقة BFF.</p></section></div></Shell>;}
