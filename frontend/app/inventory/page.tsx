import { Shell } from "@/components/shell"; import { LegacyTabs } from "@/components/legacy-tabs"; import { MaterialsManagerPanel } from "@/components/material-panels";
export default function InventoryPage() { return <Shell><div className="mb-6"><h1 className="text-2xl font-bold">المستودع</h1><p className="mt-1 text-slate-500">إضافة المواد وعرضها وإدارة كمياتها.</p></div><LegacyTabs tabs={[{label:"➕ إضافة مادة",content:<MaterialsManagerPanel mode="create"/>},{label:"📋 عرض المواد",content:<MaterialsManagerPanel mode="list"/>},{label:"⚙️ إدارة المواد",content:<MaterialsManagerPanel mode="manage"/>}]}/></Shell>; }
