"use client";
import { useRouter } from "next/navigation"; import { useState } from "react"; import { saveSession } from "@/lib/auth";
export default function LoginPage() { const router=useRouter(); const [username,setUsername]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); async function submit(event: React.FormEvent) { event.preventDefault(); setError(""); try { const response=await fetch(`${process.env.NEXT_PUBLIC_API_URL ?? "/api/v1"}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password})}); if(!response.ok){setError("بيانات الدخول غير صحيحة.");return;} const result=await response.json() as {access_token:string;refresh_token:string}; saveSession(result.access_token, result.refresh_token); router.replace("/"); } catch { setError("تعذر الاتصال بالخدمة. حاول مرة أخرى."); } } return <main className="grid min-h-screen place-items-center p-4"><form onSubmit={submit} className="panel w-full max-w-md"><div className="mb-7"><span className="grid size-10 place-items-center rounded-xl bg-brand font-bold text-white">F</span><h1 className="mt-4 text-2xl font-bold">تسجيل الدخول</h1><p className="mt-1 text-sm text-slate-500">FieldApp لإدارة العمليات الميدانية</p></div><label className="block text-sm">اسم المستخدم<input value={username} onChange={e=>setUsername(e.target.value)} className="mt-1 w-full rounded-xl border bg-transparent p-3" required/></label><label className="mt-4 block text-sm">كلمة المرور<input value={password} onChange={e=>setPassword(e.target.value)} className="mt-1 w-full rounded-xl border bg-transparent p-3" type="password" required/></label>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button className="mt-6 w-full rounded-xl bg-brand p-3 font-semibold text-white">دخول</button></form></main>; }
