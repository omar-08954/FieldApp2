"use client";
import { useState } from "react"; import { Shell } from "@/components/shell"; import { authHeaders } from "@/lib/auth";
export default function ImportPage() { const [file, setFile] = useState<File>(); const [message, setMessage] = useState(""); async function upload() { if (!file) return; const body = new FormData(); body.append("file", file); const response = await fetch(`${process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000/api/v1"}/imports/excel`, { method: "POST", headers: authHeaders(), body }); setMessage(response.ok ? "تمت معالجة الملف. الصفوف غير الصالحة نُقلت تلقائياً إلى مراجعة الاستيراد." : "تعذرت معالجة الملف؛ سجّل الدخول أولاً ثم حاول مجدداً."); } return <Shell><div className="mx-auto max-w-2xl"><div className="panel"><p className="text-sm font-medium text-brand">Excel Import</p><h1 className="mt-2 text-2xl font-bold">استيراد آمن، صفاً بصف</h1><p className="mt-2 text-slate-500">يتم تجاهل أي ID من Excel وتوليده في قاعدة البيانات. الاشتراك الناقص يصبح «غير مسجل»، والحالة الناقصة «تم الفحص».</p><label className="mt-7 flex cursor-pointer flex-col items-center rounded-2xl border-2 border-dashed border-indigo-200 p-12 text-center dark:border-indigo-900"><span className="font-semibold">اختر ملف Excel</span><span className="mt-2 text-sm text-slate-500">xlsx أو xlsm</span><input className="sr-only" type="file" accept=".xlsx,.xlsm" onChange={e => setFile(e.target.files?.[0])}/>{file && <span className="mt-4 text-sm text-brand">{file.name}</span>}</label><button onClick={upload} disabled={!file} className="mt-5 w-full rounded-xl bg-brand py-3 font-semibold text-white disabled:opacity-50">بدء الاستيراد</button>{message && <p className="mt-4 rounded-xl bg-slate-100 p-3 text-sm dark:bg-slate-800">{message}</p>}</div></div></Shell>; }
